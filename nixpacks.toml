[phases.setup]
nixPkgs = ["chromium", "chromedriver", "python311", "gcc", "openssl"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"